<h2 align="middle"><%= @nobrella_advice %></h2>

<h3 align="middle">
  <% if @nobrella_detail == "N/A" %>
      Why not add some <a href="/locations/new">locations</a> and set up your <a href="/events">weekly schedule</a>?
  <% else %>
      <a href="http://www.forecast.io/#/f/<%= @latitude %>,<%= @longitude %>">
        <%= @nobrella_detail %>
      </a>
  <% end %>
</h3>
<%= DateTime.now.change(offset: "-0500") %>
<%=  (DateTime.now.hour * 60) + (DateTime.now.min) %>
<%=  (DateTime.now.hour * 60) + (DateTime.now.min) +960%>


<%= DateTime.now.strftime('%A') %>
<%= DateTime.now.tomorrow.strftime('%A') %>
<p></p>
<%= DateTime.now.zone %>
<p></p>
<%= Time.now.strftime('%z') %>

<%= (DateTime.now.change(offset: "-0500").hour * 60) + (DateTime.now.change(offset: "-0500").min) + 960-1440 %>